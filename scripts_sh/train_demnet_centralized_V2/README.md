Centralized counterpart of the FL script V2.
Each script here corresponds to its namesake in the folder `train_demnet_FL_V2`.
It should be noted that unlike FL scripts, here (most of) the training hyperparameters are not set. These scripts use config files created by the the FL V2 scripts.
These scripts retrieve them from the config files created by the FL V2 scripts.
